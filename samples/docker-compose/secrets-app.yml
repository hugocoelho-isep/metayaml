services:
  app:
    image: myapp:latest
    ports:
      - 8080:8080
    secrets:
      - api_key
      - tls_cert
      - jwt_secret

secrets:
  api_key:
    file: ./secrets/api_key.txt
  tls_cert:
    file: ./secrets/tls_cert.pem
  jwt_secret:
    file: ./secrets/jwt_secret.txt
